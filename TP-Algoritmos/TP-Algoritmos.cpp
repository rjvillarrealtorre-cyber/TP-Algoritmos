#include <iostream>
#include <Windows.h>
#include <vector>
using std::string;

HANDLE hConsole = GetStdHandle(STD_OUTPUT_HANDLE);

#include "Personaje.h"
#include "Protagonista.h"

int main()
{
    while (true) {
        Sleep(75);
    }
}